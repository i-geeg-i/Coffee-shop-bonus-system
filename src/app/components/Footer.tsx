import styles from "./css/Footer.module.css"
const Footer = () => {
    return (
        <footer className={styles.footer}>
            It's a footer!
        </footer>
    )
}

export default Footer;